{
  "name": "agentsdraw",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @agentsdraw/app dev",
    "dev:tauri": "node scripts/dev-tauri.mjs",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
